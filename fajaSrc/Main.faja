:-) Moje prvni trida
object Main
	fields
		b, c, closure
	endFields

	methods

		def main()
			self:b <- 2
		    self:c <- 4
		    bar()
		end

		def bar()
			var sOut, sum

			sOut <- SystemIO.new
			var cl

            sum <- self:b.+(self:c)

	        self:closure <- { a |
                sOut.out(a)
	            sOut.out(sum)
	        }

:-)	        cl <- self:closure
            self:closure.call(self:b)
		end
	endMethods
endClass