:-) Moje prvni trida
object Main
	fields
		b
		c
	endFields

	methods

		def main()
			self:b <- true
		    self:c <- false
		    bar()
		end

		def bar()
			var sout, sum
			sout <- SystemIO.new
            sum <- self:b.or(self:c)
            sout.out(sum)
		end
	endMethods
endClass