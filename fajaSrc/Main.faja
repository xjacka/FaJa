:-) Moje prvni trida
object Main
	fields
		b
		c
	endfields

	methods

		def main()
			var local, local1, sout, sum
			local <- false
		    local1 <- false
			sout <- SystemIO.new
			sum <- local.not()
			sout.out(sum)
		end

		def bar()
			 self.foo(self:c)
		end
	endmethods
endclass