:-) Moje prvni trida
object Main
	fields
		b
		c
		closure
	endFields

	methods

		def main()
			self:b <- true
		    self:c <- false
		    bar()
		end

		def bar()
			var sout, sum, cl
			sout <- SystemIO.new
:-)            sum <- self:b.or(self:c)
:-)            sout.out(sum)
			sum <- 1
	        self:closure <- { a |
	            sout.out(a)
	        }
	        cl <- self:closure
            cl.call(sum)
		end
	endMethods
endClass