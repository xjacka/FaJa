class Pair
	fields
		sign
		variable
	endFields
	
	methods
		def toS()
			var sign <- " "
			var setSign <- {
				sign <- " -"
			}
			self:sign.ifFalse(setSign)
			var str <- "".+(sign).+(self:variable)
		end
	endMethods
endClass