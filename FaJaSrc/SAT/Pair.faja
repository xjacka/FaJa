class Pair
	fields
		sign
		variable
	endFields
	
	methods
		def toS()
			var sign <- " "
			var setSign <- {
				sign <- " -"
			}
			self:sign.ifFalse(setSign)
			sign.+(self:variable:id)
		end
	endMethods
endClass