package faJa.natives

import faJa.Heap
import faJa.interpreter.StackFrame

class NumberNatives {

	static plus = { StackFrame stackFrame, Heap heap ->

	}

	static minus = { StackFrame stackFrame, Heap heap ->

	}

	static mul = { StackFrame stackFrame, Heap heap ->

	}

	static div = { StackFrame stackFrame, Heap heap ->

	}

	static mod = { StackFrame stackFrame, Heap heap ->

	}

	static equals = { StackFrame stackFrame, Heap heap ->

	}

	static ifTrue = { StackFrame stackFrame, Heap heap ->

	}

	static ifFalse = { StackFrame stackFrame, Heap heap ->

	}

	static init = { StackFrame stackFrame, Heap heap ->

	}

	static toS ={ StackFrame stackFrame, Heap heap ->

	}
}
